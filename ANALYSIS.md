# Ecard-rl 全面分析报告

> 版本：2026-08-17。基于 run06–run19 全部正式数据、代码与文档。
> 所有数字均可从 `data/runs/` 下的 `runs.jsonl` / `grid.json` / `evaluation.json` 复核。

---

## 1. 项目理解

### 1.1 一句话

本项目用自博弈 PPO 模拟「皇帝牌」中的学习动力学，比较三样东西：
理性均衡（solver）、PPO 弱平稳中心（加 min-ent 正则后的动力学落点）、
心理效用扰动（前景理论 λ/τ/αβ 改变主观奖励后的落点）。

### 1.2 方法链

1. **数学层**：`solver.py` 递归求解完整 5 轮博弈，验证 p_k=q_k=1/(k+1)、V₄=-0.2、W=0.8。
2. **环境层**：`env.py` 最小 5 维状态、fail-loud、客观奖励不污染心理。
3. **效用层**：`utility.py` 实现 identity / prospect / tilt。
4. **RL 层**：`ppo.py` 双独立 actor-critic + GAE + clip + 熵正则 + min-ent hinge。
5. **扫描层**：`sweep.py` 预注册网格、元数据、弱平稳判据、多种消融开关。
6. **分析层**：`analyze.py`（seed 聚类统计）、`evaluate.py`（逐层策略 + exploitability）。

### 1.3 阶段交付

| 阶段 | 交付 | 状态 |
| :--- | :--- | :--- |
| 0–3 | 假设、环境、solver、PPO 基线 | ✅ |
| 4 | run06 主网格 + 系列消融 | ✅ |
| 5 | 人类数据 | ⬜ |
| 6 | results.md 定稿 | ✅ |

---

## 2. 核心结论（按可信度分级）

### 2.1 已验证/数学事实

- **理性均衡**：p_k=q_k=1/(k+1)，V₄=-0.2，W=0.8。
- **主观 Nash 不变性**：在当前效用层下，λ/α/β/ref 常数权重不改变主观 Nash 均衡策略。
  这是数学推导 + 数值验证，不是经验结果。
- **λ↑→p↓ 是梯度尺度效应**：adv-norm 开启后该效应消失（run18），与主观 Nash 不变性一致。
- **min-ent 0.5 不是中性稳定器**：run11_minent 显示 0.55/0.60 下 q 与 q-p 大幅上升。
- **400k 不是明显瞬态**：run19_long 的 1M 步窗口落点与 400k 一致。

### 2.2 方向性证据

- **q-p≈+0.05 在多种消融下仍为正**：真 identity、对称收益、相同初始化、共享躯干、不同更新顺序。
- **τ 对 q 无位置效应**：window 与 ref 模式均 null；但 τ→p 弱正效应（run06 +0.0044）未正式核验。
- **run15_same_init**：q-p 略降但未消失。
- **run16_update_order_random**：最终 checkpoint q-p 接近 0，提示更新相位可能是 q-p 来源之一。

### 2.3 不可用/失败

- **run12_roleswap**：训练中每轮交换导致 0/5 收敛，不能用于机制结论。
- **`--swap-eval` 反号是数学恒等式**，不携带机制信息。
- **旧 README 基线**：p=0.294/q=0.456 是历史数字，不能从当前代码复现。

---

## 3. 最新一轮结果（run15–run19）

| run | 操作 | p | q | q-p（窗口） | 弱平稳 | 关键结论 |
| :--- | :--- | :---: | :---: | :---: | :---: | :--- |
| run15_same_init | 相同初始化独立网络 | 0.206 | 0.239 | +0.033 | 4/5 | q-p 仍为正，但比 run11 小约 0.018 |
| run16_update_order_slave_first | slave 先更新 | 0.208 | 0.247 | +0.039 | 5/5 | q-p 仍为正 |
| run16_update_order_random | 随机更新顺序 | 0.209 | 0.260 | +0.051 | 5/5 | 窗口仍正；最终 checkpoint≈-0.005 |
| run17_shared_trunk | 共享躯干独立头 | 0.203 | 0.262 | +0.060 | 3/5 | 共享表征不消除 q-p |
| run18_advnorm_lambda | adv-norm 开，λ=1/5 | 0.208/0.211 | 0.253/0.263 | +0.045/+0.052 | 4/10 | λ↑→p↓ 消失 |
| run19_long | 1M 步 identity | 0.201 | 0.253 | +0.051 | 3/3 | 400k 不是瞬态 |

---

## 4. 方法学评价

### 4.1 做得好的地方

- 预测预注册（多数）。
- 对“假收敛”做自我推翻，并保留审计记录。
- 弱平稳判据同时检查漂移与振荡。
- seed 聚类意识（虽然 5 个 seed 仍是统计天花板）。
- 效用层不污染环境奖励。
- 逐层策略与 exploitability 评估补齐了“只测首轮”的短板。

### 4.2 隐藏陷阱

1. **min-ent 是强正则，不是中性稳定器**：已被 run11_minent 证实。
2. **λ 与梯度尺度混淆**：已被 run18 证实。
3. **窗口均值 vs 最终 checkpoint 不一致**：run11、run16 都暴露。
4. **5 seeds 是所有符号结论的统计上限**：单侧 p=0.031，双侧 0.0625。
5. **数据版本化缺失**：`data/runs/` 不在 git，正式数字与脚本输出有历史不一致。
6. **`--swap-eval` 曾被误用为机制证据**：已修正。

---

## 5. 统计可靠性

- **λ p 效应**：run06 中 5/5 seed 为负，但效应量（-0.023）小于末段振荡幅度。
- **adv-norm 消融**：run18 中 λ p 效应变为 +0.003，方向反转；这是目前最强的一条机制证据。
- **q-p 正号**：多个独立 run 系列均为正，但 5 seeds 的符号检验只有单侧 p≈0.031。
- **run16_random checkpoint 近 0**：n=5、均值 -0.005，需要更多 seed 确认。

---

## 6. 未解决欠账

1. **q-p 机制仍未唯一确定**：更新相位是新的候选，但窗口/checkpoint 不一致需要先解决。
2. **窗口均值与 checkpoint 的定义不统一**：需要一个一致的策略估计量（如末段集成策略）。
3. **τ→p 效应**：run06 存在弱正效应，未进入正式假设。
4. **人类数据**：完全缺失。
5. **数据版本化**：需要 SHA-256 manifest。
6. **更多 seed**：5 个 seed 不足以支撑“结论级”表述。

---

## 7. 修改建议

### 高价值低成本

1. 统一“落点”定义：窗口均值、末段集成策略、最终 checkpoint 三选一，并全项目一致使用。
2. 把 τ→p 效应写成正式假设并补一次简单核验。
3. 对 run16_random 补 seeds 到 10–15，确认 checkpoint q-p 是否稳定在 0 附近。
4. 为 `data/runs/` 生成 SHA-256 manifest。

### 高价值高成本

1. 更新相位实验：固定相位 vs 随机相位 vs 每 K 步交换，配合 checkpoint 集成评估。
2. 20–30 seeds 的 identity 与 adv-norm 对照，把 λ 效应/q-p 从“方向性”升级为“结论”。
3. 人类数据（10×100 局）。

### 低价值

- 继续扩大 λ×τ 网格。
- 继续追 run09_asym。
- 重新按原设计跑 run12_roleswap。

---

## 8. 如果重来一遍

最值得重新考虑的是：**在写“λ↑→p↓”等预测之前，先用 solver 同款递归求主观 Nash**。
这会把整个阶段 4 从“找心理均衡位移”重新定义为“测 PPO 对效用尺度的动力学响应”，
并提前把算力分配给 min-ent 敏感性、adv-norm 对照和更多 seed。
