# 方向：用 PPO 模拟赌徒，不是复现 PPO

你的真实目标不是「复现 PPO」，是用 PPO 模拟赌徒——这两个目标差着一条银河。先把这条说清楚，重写计划才有意义。

自博弈 PPO 是梯度下降物种，不是人类。它没有损失厌恶、没有「这把一定要赢回来」、没有小概率大收益的执念。用原始奖励训练它，得到的只是 PPO 自己的动力学吸引子——就是你 README 里那个 p=0.294、q=0.456 的东西。要模拟赌徒，你必须把心理显式写进收益函数，让 agent 最大化主观效用，而不是客观金钱。这是整个重写最关键的一个设计决定，后面所有步骤都围着它转。

这个游戏的妙处：+1/-5 的结构天然制造了两个心理位置。皇帝是「大输家位」——输一把亏五倍；奴隶是「大奖位」——赢一把赚五倍。前景理论说得很清楚：损失厌恶（λ>1）会让皇帝不敢出王牌、躲开 A-A；概率权重扭曲会让奴隶高估那个小概率大收益，更爱出王牌。你的旧结果里 q=0.456 > p=0.294 已经隐隐有这种不对称的影子——但那是优势归一化制造的假象，不是心理。重写后你会第一次真正区分这两条机制。

这个项目能产出的、别人没做过的东西，是一张 (λ, τ) 相图：不同心理参数下，自博弈收敛到哪里、皇帝客观期望是正是负。这才是「现实中的均衡」的诚实版本。

## 项目地图

全部产出就三样：研究问题（Question.md）、精确解（solver.py）、实验与分析（results.md + (λ, τ) 相图）。

| 阶段 | 交付物 | 状态 | 验证门 |
| :--- | :--- | :---: | :--- |
| 0 | Question.md | ✅ 内容就绪（git commit 待你做） | 三个问题 ×（假设/验证门/如果错了） |
| 1 | env.py + test_env.py | ✅ 完成（8/8 测试通过） | 规则表断言 + 非法动作 fail loud |
| 2 | solver.py | ✅ 完成（3 个验证门通过） | 1/7、V_4=-0.2、p_k=1/(k+1)、W=0.8 |
| 3 | utility.py + ppo.py + train.py | 🟡 初版完成（AI 写，待你重写） | identity 下 p̂、q̂ 对照均衡 (0.2, 0.2) |
| 4 | 扫描数据 + (λ, τ) 相图 | ✅ run05 完成（23/60 收敛，全部内部） | λ×τ 网格 × 5 seeds，预测先写 |
| 5 | 1000 局人类数据 | ⬜ 待做 | 记录每轮出牌，不是只记胜负 |
| 6 | results.md | 🟡 草稿已建（2026-08-13） | 每行：预测 → 实际 → 一句话解释 |

## 已知答案速查（solver 已验证，2026-08-09）

单轮静态（README 的旧基准）：p=q=1/7≈0.1429，V=+1/7≈0.1429。

完整 5 轮，皇帝视角（solver.py 输出）：

| k（双方剩余平民） | V_k | p_k=q_k |
| :---: | :---: | :---: |
| 0 | -5 | 1.0（强制 A-A） |
| 1 | -2 | 0.5 |
| 2 | -1 | 1/3 |
| 3 | -0.5 | 0.25 |
| 4 | **-0.2** | **0.2** |

闭式：p_k = 1/(k+1)；均衡总胜率 W = 0.8（皇帝 80% / 奴隶 20%）。

旧 PPO 对照（seed=42，6M 回合）：胜率 0.7972、期望 -0.217、首轮 p=0.2936、q=0.4556。

这些数字是阶段 3 的 identity 对照基准、阶段 4 相图的基准线、阶段 6 的讨论起点。别让它们被覆盖。

## 阶段 0：研究问题（完成）

交付物 Question.md：三个问题分开写——理性均衡、PPO 原始动力学、心理均衡。每个问题四段式：问题 → 可证伪假设（带数字）→ 验证门 → 如果错了。

完成标志：能向 reviewer 复述三个假设，并解释为什么每个都能被打脸。

规则：AI 只当 reviewer；写不下来就不碰代码；写完 git commit 一次。

## 阶段 1：环境重写（完成）

env.py（119 行）+ test_env.py（8 个测试，全部通过）。状态 = 5 维最小马尔可夫状态；非法动作 fail loud；奖励保持客观 +1/-5/0。

验证门（已全部通过）：A-A → 奴隶胜 -5；A-C / C-A → 皇帝胜 +1；C-C → 平局继续；第 5 轮强制 A-A；终局后 step 抛 RuntimeError。

规则：心理不进环境，放在阶段 3 的效用层；旧 ppo.py 只当参照物，别删，别模仿。

## 阶段 2：精确解（完成，但有一笔欠账）

solver.py（91 行），三个验证门全过：单轮静态给出 1/7 和 +1/7；完整 5 轮 V_4=-0.2、首轮 p=q=0.2、每层满足闭式 p_k=1/(k+1)；均衡胜率 W=0.8。

关键设计：状态坍缩——任何王牌出现即终局，非终局状态只有「双方各剩 k 张平民」这 5 个；每层解一个 2×2 零和矩阵博弈（通用 solve_2x2：先查纯策略鞍点，再无差异方程）。别在这个问题上加复杂度。

欠账：这份代码是 AI 写的，违反了「代码自己写」。看完后关掉文件，凭记忆重写一遍，跑同一套验证门。重写通过，阶段 2 才算真正完成。

## 阶段 3：PPO 核心（初版完成，待凭记忆重写）

交付物：utility.py（identity → prospect → tilt）+ ppo.py + train.py。目标：actor-critic 64 维、GAE、clip、熵正则，两个 agent，PPO 本体 ~180 行。

硬规则：

1. 优势归一化默认关掉、做成 flag——它是旧结果的元凶，要单独研究；
2. identity 先跑通，再碰 prospect；
3. 双日志：每步同时记主观效用和客观金钱——「主观觉得自己在赢、客观一路亏」就是赌徒；
4. 单 seed 不许写「收敛」两个字。

验证门：identity 效用下，PPO 末段窗口内的首轮 p̂、q̂ 与均衡 (0.2, 0.2) 对照，把偏差方向和大小记录下来。逼近不了，先区分 bug vs 自博弈固有的非平稳性，把这个区分写进 results.md——这个区分本身就是学习。

初版（2026-08-09）：utility.py(107) + ppo.py(236) + train.py(110)。冒烟跑通（50k 步 seed=42），结果按阶段 3 规则只记录不下结论，存在 data/stage3_*.json。你的重写清单：看懂 → 关掉文件 → 凭记忆重写 → 跑同一套验证门。

## 阶段 4：心理参数扫描（核心实验）

网格：λ ∈ {1, 2, 3, 5} × τ ∈ {0.5, 0.75, 1.0}，每格 5 个 seed。

τ 不用 0.1：T&K 单参数权重函数在 γ<~0.3 退化（w(p)≈0，奖励信号被抹平、熵正则主导）。run02 的 τ=0.1 列就是被这个退化污染的，见 debug/2026-08-11_run02_remediation.md。

记录：最终 p、q、皇帝客观期望收益、主观效用、末段漂移。

预测先写（阶段 0 的假设）：λ↑ → 皇帝首轮 p↓（损失厌恶让他躲 A-A）；τ↑ → 策略逼近均匀随机。

输出一张相图：横轴 λ、纵轴 τ、颜色 = 皇帝客观期望收益。这张图就是「现实均衡」的第一版答案。

扩展方向：让皇帝和奴隶用不同的 λ——这个游戏的心理不对称本来就该不对称。

初版（2026-08-09）：sweep.py 已建，--demo 冒烟跑通。皇帝只变 λ（无概率权重）、奴隶只变 τ（滑动窗口概率权重）——隔离两个效应。预测写在 data/sweep/predictions.json，跑完 grid.json 自动对照核验。你的重写清单：看懂 → 关掉文件 → 凭记忆重写 → 重跑 --demo 和正式网格。

run03（2026-08-12）：τ 网格已改为 {0.5, 0.75, 1.0}（γ=0.1 退化废弃）。两条预测均「不成立（方向相反，未收敛）」；τ 轴单调当时是全项目最干净的结果（注：后经 probe_minent/run05 证明是熵塌缩假象，见 debug/2026-08-15_run05.md）。收敛探测用 `--cells "1-0.5,1-1.0,5-0.5,5-1.0" --seeds "42,43" --steps 400000`，详见 results.md。

run05 前哨（2026-08-14）：probe_minent 证明边界塌缩是熵病理——min-ent 0.3 下 identity 角近似通过阶段 3 验证门（p≈0.22、q≈0.29），λ/τ 效应消失。正式网格命令：`python sweep.py --name run05 --steps 400000 --min-ent 0.3`。

run05（2026-08-15）：60/60 格落在均衡邻域，23/60 过收敛门；λ 弱支持、τ 无位置效应（只影响收敛质量）；q 系统性偏高 ~0.09，待查。相图已可出正式版。

## 阶段 5：找数据，别只靠理论

没有人类数据，「现实中的均衡」只能是假设——这一点必须写进结论。

最低成本方案：10 人 × 100 局 = 1000 局，记录每轮出牌（不是只记胜负）。直接和模拟对比。

没条件就退而求其次：用漫画里可观察的行为模式做定性锚点，结论写明「定性对应，非定量验证」。

真实数据永远比你的故事可靠。

## 阶段 6：写分析（比写代码难）

results.md，每行实验：预测 → 实际 → 一句话解释。

三个必答问题：

1. 为什么旧项目皇帝胜率 79.7% 却期望为负？
2. 哪些心理参数能复现人类式行为？
3. 「现实均衡」在这个项目里指什么，证据边界在哪？

一个待讨论的观察：旧运行胜率 0.7972 vs 均衡 0.8、期望 -0.217 vs -0.2——结果统计贴着均衡，策略（0.294/0.456）却离均衡很远。巧合还是鲁棒性？别下结论，写下来。

## 规则（唯一不变的几件事）

1. 代码自己写，AI 只当 reviewer。AI 写过的东西（如 solver.py），看完要凭记忆重写一遍。
2. 卡住超过 30 分钟可以问 AI，只许要线索，不许要代码。
3. 预测写在实验之前。实验后改预测 = 讲故事，不是做研究。
4. 单 seed 不说收敛，单次运行不写结论。
5. 心理写在效用层，不污染环境。
6. 旧 ppo.py 是参照物，不是样板。

## 文件结构（当前进度）

Question.md（阶段 0，完成）+ env.py（阶段 1，完成）+ test_env.py（阶段 1，完成）+ solver.py（阶段 2，完成）+ utility.py + ppo.py + train.py（阶段 3 初版）+ sweep.py（阶段 4 初版）+ results.md（阶段 6）+ PROJECT.md（项目整体说明）。

代码总量目标 ≈ 550 行以内；当前含阶段 4 sweep.py 已超（env 120 + test_env 73 + solver 91 + utility 107 + ppo 236 + train 110 + sweep 358 ≈ 1095 行），仍是旧 ppo.py（1758 行）的约 60%。

## 学习资料速查（完整列表见 Question.md §4）

- 博弈论：Game Theory 101（混合策略算法）、Osborne 教材、von Neumann 最小最大定理讲义；
- 强化学习：Spinning Up（PPO 页）、Sutton & Barto 第二版、CleanRL 单文件 ppo.py、Hugging Face Deep RL Course；
- 前景理论：Kahneman & Tversky 1979（λ）、Tversky & Kahneman 1992（概率权重 γ≈0.61）、《Thinking, Fast and Slow》（娱乐）；
- 自博弈：Albrecht et al. 2024《Multi-Agent Reinforcement Learning》、AlphaZero 论文。

六个阶段走完，你拥有的不是一段能跑的训练脚本，是「皇帝牌博弈 + 前景理论 + PPO」三者关系的理解。Gradient descent 写不出这段理解。I'm sorry.
