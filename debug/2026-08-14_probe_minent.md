# 2026-08-14 probe_minent 结果记录（min-ent 0.3 消融，400k 步 × 4 格 × 2 seeds）

## 一句话

最小熵约束消除了边界塌缩：收敛 0/8 → 3/8，漂移降一个量级，全部格子落在理性均衡附近（p≈0.21~0.23、q≈0.27~0.29）。run03 的 τ/λ 效应被证明是熵塌缩假象。

## 事实

- meta：min_ent=0.3、steps=400000、window 模式、git 142883ad、git_dirty=True（跑时未提交）、哈希与当前代码一致。
- |drift_p| 均值 0.040（对照 probe_window 0.329），|drift_q| 均值 0.085（对照 0.290）。
- 与均衡距离：|p-0.2| 均值 0.022；|q-0.2| 均值 0.080（q 系统性偏高 ~0.08）。
- λ=1/τ=1（identity 角，两个种子）：p=0.222/0.241、q=0.287/0.292、胜率 0.795/0.797、期望 -0.219/-0.232——对照阶段 2 均衡 (0.2, 0.2, 0.8, -0.2)，阶段 3 的 identity 验证门近似通过。
- λ、τ 效应消失：四个角点的末段窗口均值 p=0.206~0.233、q=0.268~0.290，无梯度。

## 修正的结论

1. run03 的「τ 轴单调」（q 0.12→0.07→0.04、熵 0.34→0.27→0.17）是熵塌缩梯度：τ=1 时熵更低、更快塌向 q→0。min-ent 下 q 不再随 τ 变化。
2. run03 的「λ=5 皇帝 p 跳变」（0.10→0.32）是边界吸收的产物：min-ent 下 p 在所有 λ 下持平 ~0.21。
3. 「PPO 到不了理性均衡」修正为：无熵约束时到不了（塌缩），min-ent 0.3 时能到（identity 角近似通过）。

## 下一步

- 全网格 run05：`--min-ent 0.3`，400k 步（约 3.6h）或 200k（约 1.8h）。
- 先 `git add -A && git commit`（git_dirty 已两次）。
- 跑完出正式相图并定稿 results.md；`--weight-mode ref` 消融留到 run05 之后。
