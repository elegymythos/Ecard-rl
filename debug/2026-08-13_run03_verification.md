# 2026-08-13 run03 结论核验与后续操作记录

## 先前结论核验

| 结论 | 核验方法 | 结果 | 状态 |
|------|----------|------|------|
| γ=0.1 使 T&K 权重退化 | 重算 w(1/64,0.1)、乘子、+5 主观值 | w=0.0042，乘子 0.269，+5→1.11 | ✅ 正确 |
| run03 τ 轴单调（τ↑ → q↓、熵↓） | 重算 τ 中位数 | q 0.110/0.076/0.042；熵 0.342/0.266/0.173 | ✅ 正确 |
| λ↑→p↓ 反向，λ=5 跳变 | 重算 λ 中位数 | 0.106/0.097/0.094/0.319 | ✅ 正确 |
| 未收敛（1/60，漂移大） | 重读 grid.json | n_converged=1/60，\|drift_q\| 均值 0.29 | ✅ 正确 |
| **运行代码与提交版本不一致** | CRLF 归一后重算哈希 | run02/03 的 meta 哈希 = 提交文件 CRLF 版哈希 | ❌ 原结论错误，已修正：代码一致，只是哈希函数对换行敏感 |

## 本轮完成的操作（全部为非训练操作）

1. `sweep.py`：`_file_sha` 归一化 `\r\n` 再算哈希——跨平台哈希现在一致。
2. `sweep.py`：新增 `--cells`（如 `"1-0.5,5-1.0"`）与 `--seeds`（如 `"42,43"`）选择器——探测实验不必跑全网格。
3. `results.md`：阶段 6 草稿，含预测核验、收敛性证据边界、下一步命令。
4. `direction.md` / `PROJECT.md`：进度与文件地图同步。
5. 冒烟验证：pytest 8/8；`--cells/--seeds` 4 次运行通过；meta 哈希与归一化哈希一致；验证产物已清理，data/sweep 恢复为 run01。

## 遗留（需要你跑的训练操作）

- `probe_window` / `probe_ref`：4 格 × 2 seeds × 400k 步（命令见 results.md §4）。
- 依据探测结果决定全网格 run05 或改为动力学统计。
- 阶段 5 人类数据（10 人 × 100 局）——模拟基线稳定后再做。
