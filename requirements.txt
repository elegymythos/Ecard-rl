# 运行时依赖：项目直接 import 的包，不包含传递依赖。
# 版本只设下限，不锁死——锁版本是部署阶段的事，不是学习项目的事。
#
# 测试组合（torch_env，2026-08-09）：torch 2.13.0+cpu / numpy 2.4.6 /
# matplotlib 3.11.1

numpy>=2.0
torch>=2.3
matplotlib>=3.9

# 注意：Linux 上 PyPI 默认的 torch 会附带 CUDA 依赖。
# 纯 CPU 安装方式：
#   pip install torch --index-url https://mirror.sjtu.edu.cn/pytorch-wheels/cpu/
# 完整安装：pip install -r requirements.txt
