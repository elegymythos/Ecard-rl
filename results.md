# results.md（阶段 6 草稿，2026-08-13）

> 状态：草稿。结论分级：✅ 已验证 ｜ 🟡 方向性成立但未收敛 ｜ ⬜ 待验证。

## 1. 已验证（数学与基线）

- 阶段 2 精确解（solver.py 三个验证门）：单轮静态 p=q=1/7、V=+1/7；完整 5 轮 V_4=-0.2、首轮 p=q=0.2、每轮 p_k=1/(k+1)、均衡胜率 W=0.8。
- 旧基线复现：data/checkpoints（6M 局、seed 42、17 维状态）实测 p=0.2936、q=0.4556、胜率 0.7972，与 README 逐位一致。

## 2. 阶段 4 扫描（以 run03 为准）

run01/02 有管道与网格问题（单点快照、γ=0.1 退化），只作历史，不作证据。

### 预测核验（预测 → 实际 → 解释）

1. **PPO 不停在理性均衡**：✅ 成立。60 格中只有 1 格在窗口指标下接近均衡（λ=5/τ=0.75/seed45：p=0.144、q=0.350），且该格也未真正落在 (0.2, 0.2)。
2. **λ↑ → 皇帝 p↓**：🟡 不成立（方向相反，未收敛）。p 中位数 λ=1..5 = 0.106 / 0.097 / 0.094 / 0.319——前三个持平，λ=5 跳升。候选机制（待验证）：奴隶被 τ 逼成胆小（q≈0.05）时，皇帝出王牌的局部收益 1-6q>0，自博弈让损失厌恶被对手适应盖过。
3. **τ↑ → 策略逼近均匀随机**：🟡 不成立（方向相反，未收敛）。初始策略熵中位数 τ=0.5/0.75/1.0 = 0.342 / 0.266 / 0.173——τ 越大越确定，τ=0.5（强权重）最接近均匀。τ 轴在四条 λ 列全部单调，是目前最干净的结果。
4. **概率权重 γ=0.1 废弃**：✅ 数学退化。w(1/64, 0.1)=0.0042，胜事件乘子 0.269，+5 主观值只剩 1.11。run01/02 的 τ=0.1 列是奖励信号死亡 + 熵正则主导，不是心理效应。γ 有效范围 [0.5, 1.0]。

### 收敛性（最大的证据边界）

- n_converged = 1/60（tol=0.05）；|drift_q| 均值 0.29、最大 0.42。
- 轨迹循环：λ=5/τ=1 的 q 序列 0.67→0.82→0.70→0.32→0.03→0.02→0.04。
- 40k 步/格是中途快照。所有「方向」结论都带着未收敛括号，机制解释一律标注「待验证」。

### 管道验证

- subj_std 随 τ 单调（1.77 / 1.37 / 0.91），γ 修复生效。
- 哈希跨平台问题已修复（CRLF 归一）；run02/03 实际运行代码与提交版本一致（此前不一致疑云已排除）。

## 3. direction.md 三个问题的暂态回答

1. 为什么旧项目皇帝胜率 79.7% 却期望为负？——胜率与期望由 +1/-5 的不对称决定：W=0.8 时期望恰为 -0.2。旧运行（0.797 / -0.217）的结果统计贴着均衡值，策略（0.294/0.456）却远离均衡。定稿待阶段 6。
2. 哪些心理参数能复现人类式行为？——模拟侧只有 τ 轴的单调效应和 λ=5 的跳变；人类数据（阶段 5）未做，无法回答。
3. 「现实均衡」指什么、证据边界在哪？——系统未收敛，只能称「有限预算动力学」；无人类数据，不能称「现实」。

## 4. 下一步（训练部分由你跑）

```bash
# 探测：4 个代表格 × 2 seeds × 400k 步，window 与 ref 各一组
python sweep.py --name probe_window --cells "1-0.5,1-1.0,5-0.5,5-1.0" --seeds "42,43" --steps 400000
python sweep.py --name probe_ref --cells "1-0.5,1-1.0,5-0.5,5-1.0" --seeds "42,43" --steps 400000 --weight-mode ref
```

判定：漂移随步数衰减 → 收敛，重跑全网格出正式相图；漂移不衰减 → 极限环，交付物改为动力学统计（均值/振幅/周期）。
