# results.md（阶段 6 草稿，2026-08-13）

> 状态：草稿。结论分级：✅ 已验证 ｜ 🟡 方向性成立但未收敛 ｜ ⬜ 待验证。

## 1. 已验证（数学与基线）

- 阶段 2 精确解（solver.py 三个验证门）：单轮静态 p=q=1/7、V=+1/7；完整 5 轮 V_4=-0.2、首轮 p=q=0.2、每轮 p_k=1/(k+1)、均衡胜率 W=0.8。
- 旧基线复现：data/checkpoints（6M 局、seed 42、17 维状态）实测 p=0.2936、q=0.4556、胜率 0.7972，与 README 逐位一致。

## 2. 阶段 4 扫描（以 run03 为准）

run01/02 有管道与网格问题（单点快照、γ=0.1 退化），只作历史，不作证据。

### 预测核验（预测 → 实际 → 解释）

1. **PPO 不停在理性均衡**：🟡 修正。无熵约束时确实不停（塌缩/游荡）；min-ent 0.3 后 60/60 格落在均衡邻域（run05），identity 角近似通过阶段 3 验证门。结论改为「取决于熵约束」。
2. **λ↑ → 皇帝 p↓**：🟡 弱支持。run05 中 p 均值 0.232（λ=1）→ 0.199（λ=5），中位数 0.239→0.214，方向符合预测但效应量仅 ~0.03；run03 的 λ=5 跳变是熵塌缩假象。
3. **τ↑ → 策略逼近均匀随机**：❌ 无效应。run05 中 q 在 τ=0.5/0.75/1.0 下为 0.288/0.284（差值噪声级）；run03 的单调性是熵塌缩梯度。τ 唯一残留效应是收敛质量（τ=0.5 格 2/15 过门，τ=1 格 12/15）。
4. **概率权重 γ=0.1 废弃**：✅ 数学退化。w(1/64, 0.1)=0.0042，胜事件乘子 0.269，+5 主观值只剩 1.11。run01/02 的 τ=0.1 列是奖励信号死亡 + 熵正则主导，不是心理效应。γ 有效范围 [0.5, 1.0]。

### 收敛性（最大的证据边界）

- 40k 网格：n_converged = 1/60（tol=0.05）；|drift_q| 均值 0.29、最大 0.42。
- 400k 探测（probe_window，4 格 × 2 seeds）：**0/8 收敛**，漂移不衰减。λ=1 大振幅游荡；λ=5 种子彩票式塌缩到边界角点并卡死（p=q=1 时皇帝稳定 -5；同格两种子可落在相反角点）。run03 的「唯一收敛格」是 40k 中途瞬态。
- 病理信号：奴隶 q=1 时皇帝最优反应是 p=0，但部分种子卡在 p=1（保证最大亏损）——熵塌缩，不是博弈动力学；待 --min-ent 消融确认。
- **min-ent 0.3 消融（probe_minent）**：塌缩消失，3/8 收敛，|drift_p| 均值 0.04。identity 角（λ=1/τ=1）落在 p≈0.22~0.24、q≈0.29、胜率 0.80、期望 -0.22——阶段 3 验证门近似通过。结论：边界塌缩是熵病理；run03 的 λ/τ 方向性效应也是该病理的产物。
- **正式全网格（run05，400k 步，min-ent 0.3）**：23/60 收敛；60/60 格内部且全部落在均衡邻域（p 0.18~0.25、q 0.26~0.31、胜率 0.75~0.80）。λ 效应弱但方向符合预测；τ 对位置无效应、只影响收敛质量。系统性 q 偏高（identity 角 q-0.2=+0.086）为残留训练偏差，待查。

### 管道验证

- subj_std 随 τ 单调（1.77 / 1.37 / 0.91），γ 修复生效。
- 哈希跨平台问题已修复（CRLF 归一）；run02/03 实际运行代码与提交版本一致（此前不一致疑云已排除）。

## 3. direction.md 三个问题的暂态回答

1. 为什么旧项目皇帝胜率 79.7% 却期望为负？——胜率与期望由 +1/-5 的不对称决定：W=0.8 时期望恰为 -0.2。旧运行（0.797 / -0.217）的结果统计贴着均衡值，策略（0.294/0.456）却远离均衡。定稿待阶段 6。
2. 哪些心理参数能复现人类式行为？——模拟侧只有 τ 轴的单调效应和 λ=5 的跳变；人类数据（阶段 5）未做，无法回答。
3. 「现实均衡」指什么、证据边界在哪？——系统未收敛，只能称「有限预算动力学」；无人类数据，不能称「现实」。

## 4. 下一步（训练部分由你跑）

```bash
# 查 q 偏差：identity 格 × min-ent ∈ {0.1, 0.3, 0.5} × 3 seeds × 100k
python sweep.py --name qbias --cells "1-1.0" --seeds "42,43,44" --steps 100000 --min-ent 0.1
python sweep.py --name qbias --cells "1-1.0" --seeds "42,43,44" --steps 100000 --min-ent 0.3
python sweep.py --name qbias --cells "1-1.0" --seeds "42,43,44" --steps 100000 --min-ent 0.5
```

判定：若 q 偏差随 min-ent 移动 → 地板导致的偏差；若不动 → critic/自博弈偏差。随后阶段 5 人类数据与 results.md 定稿。
